/// @brief this is a placeholder file that is always touched by cmake on every build
/// Therefore, any file including timestamp.h will always be rebuilt on every recompile
/// This allows the __DATE__ and __TIME__ macros to be updated with the build
